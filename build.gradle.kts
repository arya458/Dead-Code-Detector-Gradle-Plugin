// Root aggregator – samples only.
// Plugin implementation lives in the included build: gradle-plugin/

plugins {
    // no root plugins required
}

allprojects {
    group = "io.github.arya458"
    version = "0.1.0-early"
}

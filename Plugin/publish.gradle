plugins {
    id 'com.gradle.plugin-publish' version '1.2.1'
}

pluginBundle {
    website = 'https://github.com/arya458/dead-code-detector'
    vcsUrl = 'https://github.com/arya458/dead-code-detector'
    tags = ['gradle', 'deadcode', 'detector', 'cleanup', 'resources']

    developers {
        developer {
            id = 'aria-danesh'
            name = 'Aria Danesh'
            email = 'aria.danesh.work@gmail.com'
        }
    }
}
